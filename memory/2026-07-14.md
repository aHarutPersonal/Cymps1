# 2026-07-14

- Diagnosed deferred book guides that remained unavailable: catalog Celery tasks created and closed a new event loop per task while reusing a Gemini aiohttp client bound to a previous loop, causing immediate timeout-context failures and also poisoning later prefetched lesson work in the same worker.
- Changed catalog tasks to the persistent worker async runtime, scoped the warm Gemini client to its event loop, scheduled bounded retry wake-ups, and added Celery Beat to the local launcher.
- Added cached single-flight guide resolution with sparse bounded polling and a disabled Preparing state in the mission and lesson-reader UIs, preventing repeated resolve request storms.
- Live-recovered the reported guide: the first 2,080-word draft was rejected by quality gates and the fallback published a 3,480-word passing guide. Verification reached 398 backend and 138 Flutter tests, with static checks and a release web build passing.
- Added autonomous idle-time catalog discovery that alternates bounded Google Books and Wikidata candidates, seeds one deduplicated low-priority job only when user/catalog work is idle, and preserves the existing daily LLM budget. Split interactive, catalog, and catalog-control workers so long background generation cannot consume user worker slots.
- Tightened autonomous publication: idol evidence must match exact stored source chunks, proactive idols require verified freely licensed Commons photos with attribution, and re-ingestion replaces legacy achievements instead of duplicating them. Book guides now use a restrained nonfiction-editor voice plus hard natural-prose/structure/grounding gates. Added deterministic fallback book/idol candidate pools for temporary public-search throttling; direct source and image checks still run before publication. Full backend verification reached 441 passing tests.
- Diagnosed a book card that appeared stuck: the scheduler leased two requested books while the catalog worker had one slot, so the second waited behind a 156-second quality-fallback generation and outlived the client's 100-second poll window. Matched catalog concurrency to the two-job dispatch batch and extended sparse client polling to nearly seven minutes; the reported Franklin guide was confirmed published at 3,308 words with a passing quality report.
- Added two sourced “learning from role models” cards to the post-intake Mentor Lab carousel: Seneca and Isaac Newton, each with an offline CC0/public-domain portrait, a visible primary-source citation, asset attribution, and widget regression coverage.
- Upgraded the Mentor Lab's Newton artwork to a clean public-domain head-shot crop and added Warren Buffett's Columbia-sourced warning to choose heroes carefully, using a public-domain SelectUSA portrait with a full-face blurred-backdrop treatment.
- Fixed the post-save book-note crash: the note sheet had disposed its text controller before the reverse route animation ended while a selection overlay remained active. The sheet now owns the controller for its full lifecycle, clears selection before opening, updates the parent only after closing, and scrolls safely above the keyboard; all 141 Flutter tests pass.
- Changed the simulator launcher to compile the deployed API base by default while retaining an explicit localhost override. Verified remote health/readiness and API routing, then cleanly relaunched Flutter with the remote dart-define; switching API bases correctly cleared the old local session.
- Fixed fresh registrations inheriting the previous device user's completed-onboarding flag and CMPYS store: registration auth is now explicitly marked, and session bootstrap atomically clears the old mentor/profile/progress cache before routing the new account into onboarding. Added regression coverage; all 143 Flutter tests and static analysis pass.
- Fixed interactive plan-detail regeneration failing when google-genai retained an aiohttp session attached to a different still-live event loop. The Gemini boundary now detects the SDK transport-loop mismatch and replaces the client, with a direct regression reproduction. The Flutter detail screen also stops exposing raw provider exceptions and shows safe retry copy. Verification reached 442 backend and 143 Flutter tests, with Ruff and Flutter analysis clean.
- Fixed image-less mentor suggestions end to end: catalog candidates now retain only verified stored portraits, LLM/fallback and legacy cached candidates resolve license-verified Wikimedia photos concurrently, the suggestion API includes `image_url`, and Flutter maps, persists, and renders remote portraits with monogram fallback. Live Wikimedia resolution was verified; full verification reached 442 backend and 145 Flutter tests.
- Deployed the plan-detail async-session fix and mentor-suggestion portrait API to production as release `20260714-162123`; migrations completed, readiness reported healthy database/Redis, and the recreated Celery worker responded to ping.
