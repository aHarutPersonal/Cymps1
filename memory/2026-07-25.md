# 2026-07-25

- Completed the chat-native intake refactor: six required plan-readiness answers cover achievements, current capability, exact weekly capacity, target outcome, constraints/resources, and learning/support preferences.
- Weekly capacity is a confirmed integer from 3–60 hours; ambiguous or multi-number answers are corrected in place instead of guessed.
- Achievement intake remains explicitly self-reported with transcript provenance and is not promoted to a verified achievement record.
- Personal-plan generation now uses the session-owned intake baseline and stable strategy snapshots, with retry/lease safeguards against duplicate or mismatched results.
- Final verification passed: 574 backend tests, 184 Flutter tests, Ruff, Flutter analyze, Alembic migration, and whitespace checks.
- Diagnosed the Mentor Lab 6% production failure from the deployed API trace: an unqualified PostgreSQL `FOR UPDATE` tried to lock nullable eager-join rows during session refresh. Reused the qualified intake-session lock (`FOR UPDATE OF intake_sessions`) and added a SQL-shape regression; the backend suite now passes 575 tests. Production deployment still requires explicit approval.
- Diagnosed the Mentor Lab 58% interruption at 13:25 from production logs: the repaired backend was deployed and healthy, but Gemini ended the blueprint with `MAX_TOKENS` under its 1,000-token cap. Raised only the blueprint budget to 2,000 tokens so low-level reasoning plus the required 350–550 word document can finish; focused streaming/results tests pass. Owner approved commit, push, and rollout; revision `1d6e9ec` is live across web and workers, with the web container healthy and the public health endpoint returning 200.
- Replaced the misleading comparison percentage with a versioned like-for-like evidence contract: incompatible financial bases receive no numeric score, no-achievement baselines suppress the overall result, legacy scores are regenerated, and the UI shows either an evidence-tier point gap or an explicit insufficient-evidence state. Local verification passed 585 non-migration backend tests, 188 Flutter tests, focused Ruff checks, and Flutter analyze; deployment is pending.
- Replaced the book reader's system-only speech path with cached, expressive OpenAI audiobook narration: Expressive/Warm/Grounded presets use Marin/Cedar voices, Whisper word timestamps preserve live highlighting, upcoming sentences prefetch, speed/style persist with reading progress, and device TTS remains an announced fallback.
- Verified the narration provider end to end with a 6.04-second expressive sample and all 15 word cues. Final checks passed: 188 Flutter tests, full Flutter analysis, iOS Simulator build, 5 focused backend tests, 585 other backend tests, targeted Ruff, and whitespace checks. The standalone migration test could not connect because local PostgreSQL was stopped; full Ruff's only findings were two unrelated unused imports in an existing merge migration.
