# 2026-07-13

- Fixed plan-item infinite loading by surfacing failed/stale detail jobs and providing one explicit idempotent retry instead of automatic requeue loops.
- Fixed daily rhythms end to end: both habits and practices are classified as daily, open instantly without lesson generation, reset per day, and never gate progression.
- Fixed sequential week unlocking by hydrating current-plan status from completion records and using the first unfinished mission week for Today and Daily Focus.
- Verification reached 335 database-independent backend tests and 117 Flutter tests, plus Ruff, Flutter analysis, Python compilation, and a release web build. The migration integration test still requires a running local PostgreSQL instance.
- Replaced the post-interview comparison/blueprint waits with an automatic Mentor Lab: comparison starts on entry, the plan job is staged immediately but dispatched only after its strategy inputs, users swipe six truthful CMPYS benefit cards, and app entry unlocks only when comparison, blueprint, and the persisted plan are ready.
- Final automatic Mentor Lab verification: 340 database-independent backend tests and 119 Flutter tests passed, plus Ruff, Flutter analysis, Python compilation, and a release web build.
- Screenshot audit found two independent loops: lesson-detail polling and comparison-score session polling. The local launcher also started local services while iOS silently used the deployed API.
- Fixed local API targeting/readiness, completed-legacy mission handling, typed/bounded detail-job polling with progress, prefetch promotion with atomic worker claims, heartbeat/timeout/failure persistence, and Compare-scoped score polling.
- Final verification reached all 346 backend tests (including live migration integration) and all 123 Flutter tests, plus analysis/lint/compile/release build checks. The new detail-job `updated_at` migration is the active Alembic head.
- Comparison screenshot audit proved scoring itself had succeeded (five dimensions and five milestones were persisted), but `SessionResponse` omitted the field and FastAPI filtered it from every session response. Added the aliased response field, boundary regression coverage, bounded/typed Compare states, score persistence, and a store hydration barrier.
- Comparison repair verification reached 347 backend tests and 128 Flutter tests, with Flutter analysis clean.
- Audited weekly-time integrity: onboarding asks about capacity only through the AI interview, the extractor misses natural replies such as `5` and silently defaults to 10 hours, and the current 10-hour Week 1 contains about 5h57m of designed required work because mission details always generate three 40–60 minute lessons without receiving the mission-hour estimate.
- Implemented proactive weekly lesson preparation: Week 1 mission jobs are committed and published to the high-priority queue before the plan is exposed as ready, and the next week is idempotently prefetched at low priority after the first current-week lesson is completed.
- Replaced monolithic mission-detail generation with a small outline plus three concurrent, section-structured lesson calls. A synthetic Gemini benchmark produced three validated 1,459/1,667/1,516-word lessons in 19.5 seconds versus the historical 36-42-second monolithic LLM latency; all 390 backend tests and targeted Flutter tests passed, and Flutter analysis was clean.
