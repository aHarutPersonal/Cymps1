# 2026-07-12

- Fixed metadata-only external courses/tools/templates being misclassified as in-app reading lessons. External links now take priority, new empty content resources are not created, and legacy empty resources show a clear unavailable state instead of a blank page.
- Completed the CMPYS prompt/performance audit: blueprint is now the concise strategic verdict, the twelve-week plan is the sole execution artifact, mentor personas preserve a truthful AI-portrayal boundary, and plan/lesson/book depth contracts are enforced at runtime.
- Historical performance baseline: structured Gemini calls averaged about 20s (p50 18.5s, p95 32.1s); a standard five-turn activation now uses grounded search on 2 of 8 model responses instead of all 8, while deep lesson output remains intentionally long.
- Optimized backend pooling/logging/polling/model orchestration and Flutter splash/stream rendering/state watches; final verification reached 327 backend tests and 113 Flutter tests, plus a successful release web build. Android release build still needs an environment with the Android SDK.
