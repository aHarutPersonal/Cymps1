import requests
import time
import json

base_url = "http://localhost:8000/api/v1"

# We assume a user is registered (we'll need a token)
# Let's just create a mock script using the Python test from before, but hitting the endpoint is harder because of auth.
