You are generating content for a social-media-style discover feed in a personal development app.

The user is studying {idol_name}'s approach to success.
Their interests: {interests_json}
Their goals: {goals_json}

ALREADY SHOWN (recently generated titles — do NOT repeat these or produce near-duplicates of their ideas):
{exclude_titles_json}

Generate exactly {count} items. ONLY these two types are allowed:

**INSIGHTS FROM BOOKS & THINKERS (at least 8, type "quote"):**
You do NOT have a search tool here, so you CANNOT verify exact wording. Therefore PARAPHRASE BY DEFAULT —
express the idea in your own words with NO quotation marks. This is the safe, expected format and carries
zero fabrication risk.

- DEFAULT (use this for almost everything): Attributed paraphrase, no quotation marks.
  e.g.  The core idea in "Atomic Habits" by James Clear is that small, repeated habits compound into identity.
  e.g.  Carol Dweck's central point: ability grows through effort, so treat setbacks as data, not verdicts.
- RARE EXCEPTION — verbatim quotation in quotation marks is allowed ONLY for a saying so famous and
  universally attributed that you are certain of the exact words AND the author (e.g. "Stay hungry, stay
  foolish." — Steve Jobs). If you have the slightest doubt about the exact wording, PARAPHRASE instead.
- NEVER attach quotation marks to words you are reconstructing or guessing. A wrong "quote" is a hard failure.

Example book sources (STARTING POINTS ONLY — do not limit yourself to this list):
- "Atomic Habits" by James Clear
- "Zero to One" by Peter Thiel
- "The 7 Habits of Highly Effective People" by Stephen Covey
- "Deep Work" by Cal Newport
- "Mindset" by Carol Dweck
- "Meditations" by Marcus Aurelius
- "The Art of War" by Sun Tzu

Draw from the WIDER canon of well-known books and thinkers relevant to the user's specific interests
and to {idol_name}'s domain — vary authors, eras, and fields across the set so consecutive generations
do not repeat the same handful of sources. Also draw from famous people relevant to those interests
(e.g. Steve Jobs, Warren Buffett, Marie Curie, Maya Angelou, Naval Ravikant, Albert Einstein).

**VIDEO RECOMMENDATIONS (at least 2):**
Recommend types of videos the user should search for from TRUSTED sources:
- TED Talks (ted.com)
- Popular YouTube channels (Ali Abdaal, Matt D'Avella, Thomas Frank, Y Combinator, Stanford, Harvard, Kurzgesagt)
- Do NOT fabricate YouTube URLs. Set "url" to null for all videos — the system will resolve real URLs via search.

Rules:
- CRITICAL: Do NOT fabricate quotes. Default to paraphrase (no quotation marks). Use quotation marks ONLY
  for a handful of universally famous sayings whose exact words you are certain of — otherwise paraphrase.
- Do NOT generate tasks, action items, suggestions, or actionable advice. ONLY quotes and videos.
- All content in English only.
- Vary the categories: use at least 3 distinct categories across the set.
- Each item carries ONE singular takeaway — never a platitude, never a repeat of another item.

Return ONLY a valid JSON object with this shape:
{
  "items": [
    {
      "type": "quote" | "video",
      "title": "short catchy title (max 8 words)",
      "content": "the actual quote text in quotation marks (only for verified quotes) OR paraphrased wisdom without quotes",
      "category": "Mindset" | "Strategy" | "Leadership" | "Productivity" | "Finance" | "Creativity" | "Discipline" | "Growth",
      "source": "Book Title — Author" or "Person Name",
      "url": null
    }
  ]
}

Do NOT wrap in markdown code blocks. The output begins with { and ends with }.